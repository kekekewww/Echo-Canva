# Release Decision Evidence

The authoritative and append-only decision record is [`docs/DECISION_LOG.md`](../../docs/DECISION_LOG.md).

Key submission decisions:

| Decision | Effect on release claims |
|---|---|
| D-001 / D-002 | GPT is a control plane; deterministic code owns acoustic results and mode contracts. |
| D-006 / D-008 | Portal routing is a geometric perceptual approximation; first-order image-source reflections drive diagnostics/audio. |
| D-011 / D-013 | Browser HRTF uses a persistent graph; late reverb is a bounded stable Schroeder network. |
| D-016 | AI compilation uses strict output, validation, one repair, rate limits, timeout, and safe fallback. |
| D-018 | OpenRouter Luna is an explicit local compatibility provider, not an automatic substitute. |
| D-019 through D-031 | Hybrid 3D remains isolated/compatible and labels higher-order or atmosphere work according to validation level. |
| D-033 through D-041 | 3D direct manipulation, editable finite wall/Portal, matched route rendering, contextual workbench, and persistent cameras are presentation/authoring features over deterministic contracts. |
| D-042 | The legacy 2D-only submission snapshot is reconciled with the owner-accepted 2.5D/Hybrid 3D release candidate. |

No release document may override the authoritative decision log or convert an unvalidated research foundation into an audible/physical-accuracy claim.
