# Server-side only. The preset-only application remains available without it.
# Scene compilation always uses the required gpt-5.6 model.
OPENAI_API_KEY=
OPENAI_REASONING_EFFORT=medium
AI_REQUEST_TIMEOUT_MS=30000
