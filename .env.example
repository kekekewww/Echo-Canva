# Visitors choose OpenAI (default) or OpenRouter and provide their own key in Settings.
# Do not configure a shared deployment credential.
# Compilation uses this setting (low, medium, or high); explanation uses low effort.
OPENAI_REASONING_EFFORT=medium
# Shared Responses API timeout for compilation and explanation, in milliseconds.
AI_REQUEST_TIMEOUT_MS=30000
